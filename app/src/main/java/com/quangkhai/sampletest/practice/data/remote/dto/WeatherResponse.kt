package com.quangkhai.sampletest.practice.data.remote.dto

// ============================================================================
// EXAM: Networking — DTO + mapping (15 pts)
// ----------------------------------------------------------------------------
// @Serializable DTO(s) matching the OpenWeatherMap JSON (only fields you need,
// e.g. weather[].main and main.temp).
//   - add a toDomain() mapping to your clean Weather model
//   - target label example: "Sunny, 30°C"
// Keep JSON/DTO out of the UI — map to domain here.
// ============================================================================

// import hints:
import kotlinx.serialization.SerialName
import kotlinx.serialization.Serializable
