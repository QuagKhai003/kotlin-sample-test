package com.quangkhai.sampletest.practice.presentation.notes

// ============================================================================
// EXAM: UI — NoteEntity detail screen (10, rubric 1) + Attach Weather (rubric 3)
// ----------------------------------------------------------------------------
// @Composable to create / view a note.
//   - title + content text fields
//   - "Attach Weather Info" button -> ViewModel fetches weather, show the tag
//   - Save -> ViewModel.addNote(...), then navigate back
// ============================================================================

// import hints:
import androidx.compose.runtime.Composable
