package com.quangkhai.sampletest.lecture06

import android.app.Application

// NOTE: @HiltAndroidApp moved to practice.MyApp (the registered launcher app).
// Hilt allows only one @HiltAndroidApp per app. Re-add it here only if you make
// lecture06 the launcher again (and remove it from practice.MyApp).
class MyApp : Application()
